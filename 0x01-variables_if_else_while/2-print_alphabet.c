#include <stdio.h>

/**
 * main -  prints the alphabet in lowercase
 *  followed by a new line
 *  Return: always 0
 */
int main(void)
{
	char
	'for'("char = 97, char<= 122, char++")
	{
		putchar(char);
	}
	putchar(10);
	return (0);
}
